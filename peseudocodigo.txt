>Função recursiva()

> chama o solver

> Verificar se algum valor da solução é fracionairo:

    Se for então execute isso:
        > Escolha o valor que mais se aproxime de 0.5, se tiver dois iguais pegue o ultimo(pq fds kkkk)
        > faça duas novas variaveis, uma como a restrição de xj = 1 e outra com a restrinção de xj = 0
        > Adicione ambas na fila, vamos usar a busca por largura
        >Chame a Função recursiva() em ambas

    Senão:
        Olha se a solução é melhor do que a atual, se for:
            Salva os valores como a resposta otima atual
